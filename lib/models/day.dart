class DayWeather {
  var date;
  var iconCode;
  var high;
  var low;
  var humidity;
  var description;
  var windSpeed;

  DayWeather(this.date, this.iconCode, this.high, this.low, this.humidity,
      this.description, this.windSpeed);
}
