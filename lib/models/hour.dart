class HourWeather {
  var hour;
  var period;
  var iconCode;
  var temp;
  var humidity;
  var description;
  var windSpeed;

  HourWeather(this.hour, this.period, this.iconCode, this.temp, this.humidity,
      this.description, this.windSpeed);
}
